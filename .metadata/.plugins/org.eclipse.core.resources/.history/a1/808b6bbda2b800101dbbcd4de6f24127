<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Librería</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" th:href="@{/css/style.css}">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
</head>
<body>

<!-- Hero Section -->
<section class="hero text-center text-light d-flex align-items-center"
         th:style="'background: url(' + @{/images/hero-bg.jpg} + ') no-repeat center center; background-size: cover; height: 80vh;'">
    <div class="container">
        <h1 class="display-4 fw-bold">Descubre Mundos en Cada Página</h1>
        <p class="lead">Explora nuestro catálogo y encuentra tu próximo libro favorito</p>
        <a class="btn btn-warning btn-lg mt-3" th:href="@{/libros}">Explorar Catálogo</a>
    </div>
</section>

<!-- Libros Destacados -->
<section class="py-5 bg-light">
    <div class="container">
        <h2 class="mb-4">Libros Destacados</h2>
        <div class="row row-cols-1 row-cols-md-3 g-4">
            <div class="col" th:each="libro : ${librosDestacados}">
                <div class="card h-100 shadow-sm">
                    <img th:src="@{'/images/' + ${libro.imagen}}" class="card-img-top" alt="Libro">
                    <div class="card-body">
                        <h5 class="card-title" th:text="${libro.titulo}">Título Libro</h5>
                        <p class="card-text" th:text="${libro.autor.nombre}">Autor</p>
                        <p class="card-text fw-bold" th:text="'€' + ${libro.precio}">€0.00</p>
                        <div>
                            <i class="fa fa-star text-warning" th:each="i : ${#numbers.sequence(1, libro.rating)}"></i>
                        </div>
                        <span class="badge bg-success me-1" th:if="${libro.novedad}">Novedad</span>
                        <span class="badge bg-primary me-1" th:if="${libro.masVendido}">Más Vendido</span>
                        <span class="badge bg-danger" th:if="${libro.oferta}">Oferta</span>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Categorías -->
<section class="py-5">
    <div class="container">
        <h2 class="mb-4">Categorías</h2>
        <div class="row row-cols-2 row-cols-md-4 g-4">
            <div class="col" th:each="categoria : ${categorias}">
                <div class="card text-center h-100">
                    <div class="card-body">
                        <i class="fa fa-book fa-2x mb-2"></i>
                        <h5 class="card-title" th:text="${categoria.nombre}">Categoría</h5>
                        <p class="card-text" th:text="${categoria.libros.size()} + ' libros'">0 libros</p>
                        <a th:href="@{/categorias/{id}(id=${categoria.id})}" class="btn btn-outline-primary btn-sm">Ver</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Footer -->

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
<script th:src="@{/js/script.js}"></script>
</body>
</html>
